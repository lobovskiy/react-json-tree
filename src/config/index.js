const menuConfig = {
  items: [
    {
      url: 'table',
      icon: 'icon-table.svg',
      text: 'navigation.table'
    }
  ]
}

const tableConfig = {
  columns: [
    // 'id',
    'first_name',
    'last_name',
    'gender'
  ]
}

export {
  menuConfig,
  tableConfig
}